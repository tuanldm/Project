osMessageId																	Đặt ID Mask

osMessageQDef(<Đặt tên Queue>, <Kích thước Queue>, <Kiểu dữ liệu Queue>)									Định nghĩa 1 Queue

osMessageCreate(osMessageQ(<Đặt tên Task>), <tham số truyền vào>)										Tạo 1 luồng mới

osMessageQueuePut(<Đặt ID Mask>, &<biến gửi vào>, <Thời gian tối đa chờ nếu đầy (nếu để 0 thì nó thử đẩy đại)>, <Flag nếu sử dụng>)		Gửi dữ liệu vào Queue

osMessageQueueGet(<Đặt ID Mask>, &<biến nhận>, <Thời gian tối đa chờ nếu trống (nếu để 0 thì nó thử lấy đại)>, <Flag nếu sử dụng>)		Nhận dữ liệu từ Queue

--------------


osMessageId <Đặt tên MASK>;													osMessageId Mask_A;																
int main(){
	int a_uint32 = 22;
	int b_uint32 = 15;

	//Setup Queue
	osMessageQDef(<Đặt tên Queue>, <Kích thước Queue>, <Kiểu dữ liệu Queue>);						osMesageQDef(Queue_01, 10, uint32_t);
	<Đặt tên MASK> = osMessageCreate(osMessageQ(<Đặt tên Queue>), <Tham số truyền vào>);					Mask_A = osMessageCreate(osMessageQ(Queue_01), NULL);

	osKernelStart(); 	//Bắt đầu RTOS
	
	while(1){
		osMessageQueuePut(Mask_A, &a_uint32, 0, 0);
		osMessageQueueGet(Mask_A, &b_uint32, 0, 0);
	}
}













